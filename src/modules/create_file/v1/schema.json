{
  "metadata": {
    "workflows_module_schema_version": "1.0.0"
  },
  "fields": [
    {
      "id": "file_name",
      "type": "string",
      "description": "The name of the Google Sheet file to be created (e.g. 'MyNewSheet').",
      "label": "file name",
      "validation": {
        "required": true
      }
    },
    {
      "id": "email",
      "type": "string",
      "description": "Enter an email address to grant read and edit permissions for the new file.",
      "label": "email",
      "validation": {
        "required": false
      }
    },
    {
      "id": "google_drive_credentials",
      "type": "string",
      "label": "google-drive-credentials.json",
      "description": "Paste the entire content of your google-drive-credentials.json file here.",
      "validation": {
        "required": true
      },
      "ui_options": {
        "ui_widget": "CodeblockWidget",
        "ui_options": {
          "language": "json"
        }
      }
    }
  ],
  "ui_options": {
    "ui_order": ["file_name", "email", "google_drive_credentials"]
  }
}
